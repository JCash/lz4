--- a/tests/fuzzer.c
+++ b/tests/fuzzer.c
@@ -263,7 +263,7 @@
 }
 
 
-#ifdef __unix__   /* is expected to be triggered on linux+gcc */
+#if defined(__unix__) && !defined(__EMSCRIPTEN__)   /* native low-address mmap */
 
 static void* FUZ_createLowAddr(size_t size)
 {
